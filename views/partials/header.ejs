<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>DWOJ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css">
    <style>
        pre code { background-color: #f8f9fa; padding: 0.2rem 0.4rem; border-radius: 4px; }
        blockquote { border-left: 4px solid #ccc; padding-left: 1rem; color: #666; }
        .katex { font-size: 1.1em; }
    </style>
    <%- typeof pluginHeader !== 'undefined' ? pluginHeader : '' %>
</head>

<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">DWOJ</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="/">题目列表</a></li>
                    <li class="nav-item"><a class="nav-link" href="/status">状态</a></li>
                    <li class="nav-item"><a class="nav-link" href="/discuss">讨论区</a></li>
                    <% if(user && ROLE_LV[user.role] >= 1) { %>
                        <li class="nav-item"><a class="nav-link text-warning" href="/admin">管理面板</a></li>
                    <% } %>
                </ul>
                <ul class="navbar-nav">
                    <% if(user) { %>
                        <li class="nav-item"><a class="nav-link" href="/profile"><%= user.username %> <span class="badge bg-secondary"><%= user.role %></span></a></li>
                        <li class="nav-item"><a class="nav-link" href="/logout">注销</a></li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link" href="/login">登录</a></li>
                        <li class="nav-item"><a class="nav-link" href="/register">注册</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
